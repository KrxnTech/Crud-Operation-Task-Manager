<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Notes</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h3>✌️ Welcome ✌️</h3>
    <a href="/tasks/new">Add new Task</a> <!-- SEND REQ TO BACKEND AND TRANSFER TO THE EDIT ROUTE PAGE -->

    <!-- HTML UN ORDERD LIST -->
    <ul>
        <!-- WE R GOING TO USE FOR EACH FUNCTION -->
        <!-- WE HAVE TO INTERPOLLATION SYNTAX -->
        <% tasks.forEach(task=> { %>
            <li>
                <%= task.title %>
                    <a href="/tasks/<%= task.id %>/edit">Edit</a>

                    <!-- DELETE -->
                    <form action="/tasks/<%= task.id %>?_method=DELETE" method="POST">
                        <button type="submit">Delete</button>
                    </form>

            </li>
            <% }) %>
    </ul>
</body>

</html>